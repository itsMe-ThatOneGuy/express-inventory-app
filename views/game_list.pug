extends layout

block content
    h1= title

    ul
        each game in game_list
            li
                a(href=game.url) #{game.title}
                |  (#{game.category.name})

        else
            li thereare no games.
